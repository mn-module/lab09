<?php
    // Database connection details
    $host = "localhost";      //uns the server locally
    $user = "root";           // Default username for XAMPP's MySQL
    $pwd = "";                // Default password is empty 
    $sql_db = "exhibition"; // database name from Task 1
?>
